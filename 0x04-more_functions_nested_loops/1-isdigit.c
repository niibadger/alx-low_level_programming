#include "main.h"

/**
 *_isdigit - Checks for a digit from 0 through 9
 *
 *Return: 1 if c is a digit, 0 otherwise.
 */
int _isdigit(int x)
{
if ((x >= '0') && (x <= '9'))
return (1);
else
return(0);
return (0);
}
